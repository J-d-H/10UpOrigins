package manipulatables;

import kha.Assets;

class Injection extends UseableSprite {
	public function new(px: Int, py: Int) {
		super(Keys_text.INJECTION, Assets.images.spritze2, px, py);
	}
}
